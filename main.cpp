#include <iostream>
#include <vector>
#include <set>

int main() {
    /* max cx
     * s.t. 
     * Ax = b
     * x >= 0
    */

    
    std::vector<int> c;
    std::vector<int> b;
    // vector of columns
    std::vector<std::vector<int>> A;

    

    return 0;
}
